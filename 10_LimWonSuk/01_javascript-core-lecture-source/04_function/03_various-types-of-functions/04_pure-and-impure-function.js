/* 순수 함수와 비순수 함수
*
* - 순수함수 : 외부 상태에 의존하지 않고 외부 상태를 변경하지 않는 함수
*
* - 비순수 함수 :  외부 상태에 의존하거나 외부 상태를 변경하는 함수 (비순수는 지양)
*
* */
var cnt = 0;
function increase(n){return ++n;}
function increase(n){return --cnt;}

var cnt = 0;

// 첫 번째 함수 (죽었음 💀)
function increase(n) { return ++n; }

// 두 번째 함수 (살았음 👑)
function increase(n) { return --cnt; }

// 실행해보기
console.log(increase(100)); // 결과는?