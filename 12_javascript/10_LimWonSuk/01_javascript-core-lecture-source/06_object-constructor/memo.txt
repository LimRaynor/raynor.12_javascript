자바 상속 = 부모가 자식들에게 가지고 있는걸 나눠주는것 + 자식들은 독자적으로 추가값을 가질수 있다
상속후 부모 값을 바꿔도 자식은 안바뀜

자바 스크립트  상속 = 부모가 자식들에게 가지고 있는걸 나눠주는것 + 자식들은 독자적으로 추가값을 가질수 있다
상속후 부모 값을 바꾸면 자식값도 바뀜

언어,방식,비유,특징
Java,Class (청사진/복사),🖨️ 인쇄물 (Photocopy),
"원본(클래스)을 보고 찍어내면, 그 뒤로 원본과 별개입니다."

JS,Prototype (연결),🔗 바로가기 링크 (Live Link),
원본(부모 객체)과 줄으로 연결되어 있습니다. 원본이 바뀌면 같이 바뀝니다.

---

다른객체를 참조하는 대상을 프로토 타입이라 하기로함
